# Tibetan spellchecker firefox extension

These are the files to build the firefox extension. See [documentation](https://developer.mozilla.org/en-US/Add-ons/Extension_Packaging) for format. To build it, run

    make
